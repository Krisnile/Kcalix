# 参与开发

本文档描述 Kcalix 的日常开发与代码合入规则。个人开发也应遵循同一流程，以保证每次变更可理解、可验证、可撤回。

## 开发流程

1. 确认 `master` 与远端同步，且工作区没有无关改动。
2. 从最新 `master` 创建短期分支。
3. 只在一个分支中处理一个明确目标。
4. 完成代码后运行项目检查。
5. 检查差异，确认没有密钥、个人数据、构建产物或临时文件。
6. 创建 Pull Request，经检查通过后再合并。

分支名称使用小写英文和连字符：

```text
feat/food-search
fix/csv-import-date
docs/update-readme
chore/upgrade-expo
```

## 提交规范

提交标题采用以下格式：

```text
<类型>: <简明说明>
```

常用类型：

- `feat`：新增功能。
- `fix`：修复缺陷。
- `docs`：只修改文档。
- `refactor`：不改变外部行为的代码调整。
- `test`：增加或修改测试。
- `chore`：依赖、构建或维护工作。

一个提交应表达一个完整目的。提交前检查作者信息，不添加与实际贡献不符的联合署名。

## 本地检查

首次开发先安装锁定版本的依赖：

```bash
npm ci
```

提交前运行：

```bash
npm run check
```

该命令会执行 TypeScript 类型检查并验证 Expo 配置。自动检查通过不代表功能一定正确；涉及界面、数据导入导出或计算逻辑时，还应在真机或模拟器中验证相关操作。

## Pull Request

Pull Request 应包含：

- 变更目的和范围。
- 主要实现结果。
- 已完成的验证。
- 界面变更的截图或录屏。
- 已知限制、风险和回退方式。

不要把重构、依赖升级和新功能混在同一个 Pull Request 中，除非它们无法安全拆分。

## 版本与发布

- 版本号采用 `主版本.次版本.修订版本`。
- `package.json`、`package-lock.json` 和 `app.json` 中的版本必须一致。
- Android `versionCode` 每次发布必须递增，不得复用。
- 正式标签格式为 `v<版本号>`，例如 `v1.0.1`。
- 已发布标签不得移动、覆盖或复用；修正发布内容时应创建新版本。
- 发布前先更新 `CHANGELOG.md`，将“未发布”内容归入新版本。
- APK 必须由对应版本代码构建，并记录 SHA-256 校验值。

## 敏感信息

禁止提交以下内容：

- 密码、令牌、私钥、签名文件和证书。
- `.env` 中的真实凭据。
- 用户数据、调试导出数据或个人联系方式。
- 未经许可的第三方资源。

如果敏感信息已经进入 Git 历史，仅删除文件并不足够；应立即吊销相关凭据，并按泄露范围清理历史。
